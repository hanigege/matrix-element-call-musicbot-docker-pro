[matrix]
homeserver = "${MATRIX_HOMESERVER}"
user_id = "${MATRIX_USER_ID}"
access_token = "${MATRIX_ACCESS_TOKEN}"

[bot]
name = "Music Bot"
history_limit = 10
auto_accept_invites = true

[access]
# 单实例 musicbot 只有一个全局播放器；限制控制房间，避免陌生房间或私聊抢占播放。
# 留空表示所有已加入房间都可控制，生产环境建议填入 Element 房间 ID。
allowed_rooms = [
  "${MATRIX_ALLOWED_ROOM_ID}",
]
# 播放器是单实例全局资源；只有这些 Matrix 账号可以查看节目列表、点播或控制播放，不能随便留空开放。
admin_users = [
  "${MATRIX_ADMIN_USER_ID}",
]

[paths]
audio_dir = "/tmp/musicbot_audio"
saved_queues_file = "data/saved_queues.json"

[audio]
cache_mode = "size_lru"
cache_max_bytes = 1073741824
cache_delete_after_playback = false
cache_delete_on_shutdown = true
search_mode = "fast"
search_timeout_seconds = 8.0
extractor_retries = 1
download_format = "wav"
audio_quality = "best"
stream_first_idle = true
# 直播电台没有自然结束点；不要后台预取或失败转下载，避免 ffmpeg/yt-dlp 长时间挂住。
stream_prefetch_current = false
stream_retry_to_file_on_fail = false
auto_advance_buffer = 2.0
preroll_silence = 1.0
normalize_audio = false
fade_in_ms = 120
volume_percent = 100

[worker]
max_restart_attempts = 3
heartbeat_interval_seconds = 10.0
skip_cooldown_seconds = 1.0
stop_timeout_restart_threshold = 2
# 兼容多数 Synapse/Element Call 栈；确认 homeserver 支持 MatrixRTC sticky events 后再改 matrix2_auto。
membership_mode = "legacy"
log_max_bytes = 2000000
log_backups = 5

[playlist]
max_tracks_per_request = 50
background_load_concurrency = 4

[logging]
file = "logs/musicbot.log"
clean_enabled = true
clean_file = "logs/musicbot.clean.log"
clean_filter_matrixrtc_noise = true
max_bytes = 2000000
backups = 5

[ui]
show_progress_messages = false
rich_formatting = false
quiet_mode = true
